version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # FileInfo is intentionally the response for both Upload and GetMetadata.
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
